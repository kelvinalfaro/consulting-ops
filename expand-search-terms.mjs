#!/usr/bin/env node
import { existsSync, readFileSync } from 'node:fs';import { resolve } from 'node:path';import { fileURLToPath } from 'node:url';import yaml from 'js-yaml';
const variations={'strategic planning':['strategy consultant','strategic plan facilitation','planning facilitator','organizational strategy'],'coaching':['executive coaching','leadership coaching','team coaching','leadership development'],'facilitation':['meeting facilitation','retreat facilitator','workshop facilitator','stakeholder engagement'],'organizational development':['organization development','culture change','change management','organizational effectiveness']};
export function expandTerms(services=[]){const results=[];for(const service of services){const name=typeof service==='string'?service:service.name;results.push(`\"request for proposals\" \"${name}\"`,`\"request for qualifications\" \"${name}\"`,`\"call for consultants\" \"${name}\"`);for(const [key,values] of Object.entries(variations))if(name.toLowerCase().includes(key)||key.includes(name.toLowerCase()))for(const value of values)results.push(`\"RFP\" \"${value}\"`)}return[...new Set(results)]}
function main(){const profile=existsSync('config/company_profile.yml')?yaml.load(readFileSync('config/company_profile.yml','utf8')):{};console.log(yaml.dump({search_terms:expandTerms(profile.services??[])}))}if(process.argv[1]&&resolve(process.argv[1])===fileURLToPath(import.meta.url))main();
