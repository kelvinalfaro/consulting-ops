# Opportunity scan

Read `config/rfp_sources.yml` and the firm profile. Search enabled feeds/APIs and, when the AI has web access, run configured search terms across official procurement portals, nonprofit/foundation notices, higher education, and public-sector sources. Preserve URLs and issuer/title/deadline evidence in `data/rfp_pipeline.md`. Deduplicate against pipeline, tracker, and normalized records. Do not bypass access controls.
