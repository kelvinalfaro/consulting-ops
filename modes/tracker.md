# Pursuit tracker

Treat `data/rfp_tracker.md` as canonical. Use validated states from `templates/states.yml`, atomic writes, and opportunity IDs for deduplication. Report total, active, submitted, shortlist, win/loss, next deadlines, missing deadlines, and items needing amendment review. Never silently rewrite user notes or outcomes.
