# Batch processing

Process multiple pipeline entries with a user-defined limit. Capture per-item errors without aborting the full batch. Deduplicate first, preserve sources, serialize tracker writes, and return a ranked summary by decision, score, deadline, and confidence. Do not create proposal workspaces for No Bid or unresolved hard gates.
