#!/usr/bin/env node
import { existsSync, mkdirSync, readFileSync, writeFileSync } from 'node:fs';
import { dirname, extname, join, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';
import yaml from 'js-yaml';
function read(path){const text=readFileSync(path,'utf8');return extname(path)==='.json'?JSON.parse(text):yaml.load(text)}function slug(v){return String(v??'opportunity').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')}
export function createResearchBrief(record,root='proposals'){const opportunity=read(resolve(record));const folder=resolve(root,slug(opportunity.id??`${opportunity.issuer}-${opportunity.title}`));mkdirSync(folder,{recursive:true});const path=join(folder,'research-brief.md');const text=`# Issuer research: ${opportunity.issuer??'Unknown issuer'}\n\n**Opportunity:** ${opportunity.title??'Untitled'}  \n**Authoritative source:** ${opportunity.source_url??opportunity.source_file??'Unknown'}\n\n## Verified organization context\n\n- Mission and public priorities: [Research and cite]\n- Current strategy or plan: [Research and cite]\n- Relevant leadership and governance: [Research and cite]\n- Procurement history and incumbent signals: [Research and cite]\n\n## Stakeholders and decision process\n\n- Authorized procurement contact: ${opportunity.submission?.contact??'[Confirm from RFP]'}\n- Program owner: [Research]\n- Evaluation process: [Use RFP; do not speculate]\n\n## Bid implications\n\n- Client-language themes to mirror: [Add]\n- Supported differentiators: [Map to approved evidence]\n- Relationship position: [Known / unknown]\n- Win conditions and risks: [Add]\n\n## Sources\n\n- ${opportunity.source_url??opportunity.source_file??'[Add]'}\n`;writeFileSync(path,text,'utf8');return{path,folder}}
function main(){const record=process.argv[2];if(!record){console.log('Usage: node client-research.mjs <opportunity.yml>');process.exit(1)}console.log(JSON.stringify(createResearchBrief(record),null,2))}if(process.argv[1]&&resolve(process.argv[1])===fileURLToPath(import.meta.url))main();
