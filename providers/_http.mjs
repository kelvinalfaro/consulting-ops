export async function fetchWithPolicy(url,options={}){const response=await fetch(url,{method:options.method??'GET',headers:{'user-agent':'consulting-ops/0.2 (+local RFP discovery)',...(options.headers??{})},body:options.body?JSON.stringify(options.body):undefined,redirect:'follow',signal:AbortSignal.timeout(options.timeout_ms??20000)});if(!response.ok)throw new Error(`${response.status} ${response.statusText}`);return response}
